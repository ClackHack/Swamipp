class dictionary()
let keys=[]
let values=[]
func add(key,value)
if is_string(key) then
append(keys,key)
append(values,value)
else
println("Expected string as key")
end
end
func get(key)
for i=0 to len(keys/0) then
if keys/i == key then
return values/i
end
end
return -1
end
func pop(key)
for i=0 to len(keys/0) then
if keys/i == key then
pop(keys,i)
return pop(values,i)
end
end
return -1
end
end